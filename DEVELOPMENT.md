# Lite Tabs 长期开发理念

本文档是 Lite Tabs 的长期产品与工程约束。后续需求、重构和发布决策默认遵循本文；若改动需要突破约束，应先说明原因、收益和退出方案。

## 1. 产品定位

Lite Tabs 是 Obsidian 原生标签页的轻量侧栏视图，目标只有三个：

1. 更快地看见和定位当前已打开的标签页。
2. 用尽可能少的操作完成激活、关闭、固定和排序。
3. 在桌面端和移动端保持低开销、可预测、可长期维护。

原生工作区始终是唯一事实来源。插件不建立第二套标签、分组或工作区模型，只派生显示状态并调用原生标签操作。

## 2. 核心原则

### 极简

- 优先删除、合并或复用已有能力，再考虑增加设置和按钮。
- 不复制 Obsidian 已经提供且足够好用的命令。
- 不为低频场景引入常驻状态、后台任务或新的抽象层。
- 每项功能应可独立移除，不影响核心标签列表。

### 长期

- 兼容性、可测试性和故障隔离优先于短期功能数量。
- Obsidian 非公开运行时结构的访问必须集中在 `workspace-adapter.ts`。
- 持久化设置必须有默认值、输入规范化和向后兼容策略。
- 不依赖特定主题、内部 DOM 结构或短期平台特性。

### 高效

- 保持事件驱动，不轮询。
- 合并同一帧内的重复刷新，复用 DOM，只更新变化状态。
- 拖拽期间缓存几何信息，只在布局、滚动或尺寸变化后失效。
- 优化以真实瓶颈和可复现数据为依据，不维护推测性的长期缓存。

## 3. 不可突破的边界

- 不访问网络，不打开外部 URL。
- 不读取、写入、重命名或删除仓库文件。
- 不读取或写入剪贴板。
- 不引入云同步、账户、遥测或 AI 服务。
- 不保存标签会话、最近关闭历史、自定义分组或工作区快照。
- 不发展为文件管理器、项目导航器或完整 Workspace Manager。

安全边界由 `npm run check:boundaries` 持续检查。

## 4. 功能准入门槛

新功能只有同时满足以下条件才进入实现：

1. 解决高频、可明确复现的标签管理问题。
2. 无法通过现有命令、快捷键或少量自定义 CSS 合理解决。
3. 直接派生自原生工作区，原则上不增加持久状态。
4. 不引入运行时依赖、轮询、文件访问或网络访问。
5. 最多增加一个设置，并提供无配置的合理默认行为。
6. 有自动化测试或明确的手工回归步骤。
7. 桌面端、移动端及三种布局均有可接受的退化方式。

只改善少数外观偏好、需要同步自定义状态、批量改变大量工作区状态，或明显扩大插件定位的需求默认拒绝。

## 5. 架构约束

- `main.ts` 只负责插件生命周期、命令、设置应用和视图协调。
- `LiteTabsView.ts` 只负责 Obsidian 视图生命周期。
- `TabController.ts` 负责界面协调，不长期承载可独立测试的算法。
- 标签过滤、排序索引、结构签名和几何计算优先写成无 DOM 的纯函数。
- `workspace-adapter.ts` 是内部结构的兼容隔离层；其他模块不得扩散运行时类型断言。
- CSS 使用 Obsidian 变量和插件命名空间，不覆盖无关全局组件。

重构采用小步迁移：先建立测试，再抽离模块，最后删除旧实现。禁止无回归保护的一次性重写。

## 6. 复杂度与性能预算

- 不新增运行时依赖。
- 生产 `main.js` 原则上保持在 60 KB 以内；超出时必须说明实际收益。
- 空闲状态 CPU 占用应接近零，不允许定时轮询。
- 以 100 个打开标签作为常规压力场景，刷新不得产生明显卡顿。
- 活跃标签切换应尽量只更新前后两个条目的状态。
- 新功能若显著增加拖拽分支、布局测量或持久状态，应降低方案复杂度。

文件行数不是目标，但单个控制器持续增长是拆分信号。拆分应减少耦合和可变状态，而不是仅移动代码。

## 7. 交互与无障碍

- 鼠标、触摸和键盘应覆盖核心定位与激活流程。
- 可交互元素使用正确语义、可见焦点和准确的 `aria` 状态。
- 搜索支持快速聚焦、方向导航、Enter 激活和 Escape 清理。
- 动画只提供反馈，不承担唯一的状态表达。
- 移动端优先保证滚动与拖拽不冲突；无法可靠实现时允许功能受限，不引入隐蔽手势。

## 8. 测试与发布标准

每次合并至少通过：

```bash
npm test
npm run check:boundaries
npm run check:versions
npm run build
```

自动化测试优先覆盖纯逻辑和高风险边界：同组与跨组排序、过滤、结构变更、拖拽位置计算、设置迁移。

发布前执行精简手工矩阵：

- 桌面端：三种布局的激活、关闭、固定、筛选和跨组拖拽。
- 移动端：滚动、拖拽手柄、底部堆叠和活动标签操作。
- 边界状态：无标签、单标签、多组、长标题、非文件视图和隐藏工具栏。

版本号、清单、锁文件和 `versions.json` 必须一致。发布构建由干净环境产生，不手工编辑 `main.js`。

## 9. 长期路线

### 稳定化

- 建立常规 CI、纯逻辑测试和版本一致性检查。
- 持续从 `TabController` 抽离可测试逻辑，优先处理排序和拖拽几何。
- 完善键盘操作和无障碍语义。

### 维护

功能稳定后，以 Obsidian 兼容、性能、移动端交互和无障碍修复为主。没有高价值问题时，不以新增功能作为版本目标。

## 10. 需求决策模板

- 问题：具体在哪个重复工作流中发生？
- 频率：每天、每周还是偶发？
- 原生替代：Obsidian 或 CSS 是否已经能够解决？
- 最小方案：能否不增加设置和持久状态？
- 成本：增加哪些事件、状态、DOM、内部 API 或移动端分支？
- 验证：自动化测试和手工回归如何覆盖？
- 退出：效果不佳时能否独立删除？

无法清楚回答这些问题的需求，不进入实现。
